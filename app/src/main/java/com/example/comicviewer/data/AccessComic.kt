package com.example.comicviewer.data

/*
import android.arch.persistence.room.Dao
import android.arch.persistence.room.Delete
import android.arch.persistence.room.Insert
import android.arch.persistence.room.Query
import com.example.comicviewer.model.comic.Comic

@Dao
interface AccessComic {
    @get:Query("SELECT * FROM comic")
    val all: MutableSet<Comic>

    @Query("SELECT * FROM comic WHERE comic_title LIKE :name")
    fun findByName(name: String?): Comic

    @Insert
    fun insertAll(vararg comic: Comic)

    @Delete
    fun delete(comic: Comic)
}*/
